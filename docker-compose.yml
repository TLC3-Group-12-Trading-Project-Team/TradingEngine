version: '2'
services:
  trade-engine-prod:
    container_name: trade-engine-prod
    build:
      context: .
      dockerfile: Dockerfile
    #    image: message-server:latest
    ports:
      - 23000:23000
